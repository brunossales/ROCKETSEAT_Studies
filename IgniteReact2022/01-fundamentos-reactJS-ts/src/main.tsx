import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

// ! diz que vai estár aqui
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
